# Copy to .env.local for local overrides.

# Backend API base URL — leave blank to use Vite proxy (local dev)
# Set to your deployed API URL in production: https://your-api.onrender.com
VITE_API_URL=
